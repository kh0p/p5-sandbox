var canvas =      { w: 0, h: 0};
var col =  { r: 0, g: 0, b: 0 };

function setup() {
  canvas.w = 730;
  canvas.h = 697;

  createCanvas(canvas.w, canvas.h);
  background(col.r);
}

function draw() {}
